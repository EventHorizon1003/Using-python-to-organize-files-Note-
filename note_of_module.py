#! python3 -shebang

'''Purpose of shebang to recognize the interpreter
type when yoy want to execute the script form the shell
'''

# import sys
# sys.argv  - command line arguments

# import re
# Creating Regular Expression(Regex) object
# \d mean digit character
# eg = re.compile(r'\d ....) *r mean raw string

#import os
